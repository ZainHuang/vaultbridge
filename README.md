# VaultBridge
VaultBridge is a stateful multi-device sync plugin for Obsidian that uses GitHub as the sync backbone. It provides three-way synchronization, safe deletion and rename propagation, conflict detection, recovery, post-sync verification, and guarded auto-sync across desktop and mobile devices.
